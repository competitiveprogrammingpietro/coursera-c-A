/* 
   Coursera "C++ as a better C" 
   TODO
   Pietro Paolini
 */
#include <iostream>
#include <vector>

unsing namespace std;

template <class T> 
class Graph {
public:
  Graph(int size = 10, int density = 0, int range = 0):m_density(density), m_range(range) {
    m_graph = new vector<T>(
  }

private:
  vector<T> m_graph;
  const int m_density;
  const int m_range;

  

}
